# Terraform state files (contain sensitive data)
terraform.tfstate
terraform.tfstate.backup

# Terraform working directory
.terraform/

# Lock file (should not be shared across environments)
.terraform.lock.hcl

# Crash logs (in case Terraform crashes)
crash.log

# Ignore variable files that may contain sensitive data
*.tfvars
*.auto.tfvars

# Ignore output plan files
*.tfplan

# Ignore provider logs (if enabled)
*.log

